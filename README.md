# Flutter Web Build Output

This is the result of the web build version from the **Finager** Flutter app.

## Build Information
- **App Name**: Finager
- **Version**: 1.0.0 (Build Number: 1)
- **Package Name**: finager

## Directory Contents
This directory contains the static files generated by the `flutter build web` command:

- **`index.html`**: The entry point for the application.
- **`main.dart.js`**: The compiled Dart code required to run the application.
- **`assets/`**: Directory containing static assets (images, fonts, configuration).
- **`canvaskit/`**: Contains the CanvasKit WebAssembly binaries for the rendering engine.
- **`flutter.js` / `flutter_bootstrap.js`**: Scripts responsible for bootstrapping the Flutter engine.
- **`version.json`**: Metadata file containing build versioning information.
